"""Reusable pentest engine: scope gating, storage, scan queue and stages.

The engine is deliberately free of any Telegram dependency so it can later be
driven from a corporate web panel without touching this layer.
"""
