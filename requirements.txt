# Telegram bot framework (async). >=3.7 for DefaultBotProperties.
aiogram>=3.7.0

# Configuration
pydantic>=2.6.0
pydantic-settings>=2.2.0
PyYAML>=6.0.1

# routersploit is installed via install.sh (pip install routersploit or vendored
# clone) because it has heavy/legacy transitive deps that should not block the
# core install. See install.sh §5 and README.
